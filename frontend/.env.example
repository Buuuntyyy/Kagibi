# ===== AUTH PROVIDER =====
# "local" uses the Go backend + PostgreSQL for auth (default, recommended)
# "supabase" uses Supabase/GoTrue
# "pocketbase" uses PocketBase
VITE_AUTH_PROVIDER=local

# ===== BACKEND API =====
VITE_API_URL=https://your-domain.com/api/v1/
