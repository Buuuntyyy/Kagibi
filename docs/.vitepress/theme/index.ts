import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Extends VitePress's default theme with Kagibi's brand palette only —
// no custom components needed for a docs site this size.
export default DefaultTheme
